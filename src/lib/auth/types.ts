/**
 * Type exports for Better-Auth
 * Centralizes all auth-related types for consistency across the codebase
 */

export type { Session, BetterAuthUser as User } from './auth';
export { AuthRoles } from '../auth-roles';
